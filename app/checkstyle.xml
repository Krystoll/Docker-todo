<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
        "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
        "https://checkstyle.org/dtds/configuration_1_3.dtd">

<module name="Checker">

    <!-- Кодировка файлов -->
    <property name="charset" value="UTF-8"/>
    <property name="severity" value="warning"/>

    <!-- Макс. длина строки: 120 символов -->
    <module name="LineLength">
        <property name="max" value="120"/>
        <property name="ignorePattern"
                  value="^package.*|^import.*|a href|href|http://|https://"/>
    </module>

    <!-- Нет пустых строк в начале файла -->
    <module name="BeforeExecutionExclusionFileFilter">
        <property name="fileNamePattern" value="module\-info\.java$"/>
    </module>

    <module name="TreeWalker">

        <!-- ── Именование ─────────────────────────────── -->
        <!-- Классы: UpperCamelCase -->
        <module name="TypeName"/>
        <!-- Методы: lowerCamelCase -->
        <module name="MethodName"/>
        <!-- Локальные переменные: lowerCamelCase -->
        <module name="LocalVariableName"/>
        <!-- Параметры методов -->
        <module name="ParameterName"/>

        <!-- ── Импорты ─────────────────────────────────── -->
        <!-- Нет wildcard импортов (import java.util.*) -->
        <module name="AvoidStarImport"/>
        <!-- Нет неиспользуемых импортов -->
        <module name="UnusedImports"/>

        <!-- ── Блоки и скобки ─────────────────────────── -->
        <!-- Фигурные скобки обязательны для if/else/for -->
        <module name="NeedBraces"/>
        <!-- Нет пустых блоков catch -->
        <module name="EmptyCatchBlock"/>

        <!-- ── Методы ─────────────────────────────────── -->
        <!-- Макс. длина метода: 80 строк -->
        <module name="MethodLength">
            <property name="max" value="80"/>
        </module>

        <!-- ── Прочее ─────────────────────────────────── -->
        <!-- Нет magic numbers (кроме -1, 0, 1, 2) -->
        <module name="MagicNumber">
            <property name="ignoreNumbers" value="-1, 0, 1, 2, 100, 99, 9999"/>
            <property name="ignoreAnnotation" value="true"/>
        </module>

    </module>
</module>